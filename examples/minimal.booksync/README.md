# Minimal BookSync v1 fixture

This package contains generated text and a one-second synthetic tone. It exists only to validate the BookSync contract and is not an alignment-quality fixture.

The original source is intentionally not embedded. Its hash represents the synthetic fixture identity described by `booksync synthetic source v1`.
